{
    taskDetails(
        from: 1644796800000
        to: 1676246400000
        aggregations: [
            { field: "activities.nodes.id", type: count, name: "count" }
        ]
    ) {
        tasks {
            id
            activities {
                totalCount
                nodes {
                    id
                }
            }
            aggregation {
                name
                value
            }
        }
        pageInfo {
            hasNextPage
            endCursor
        }
    }
}
